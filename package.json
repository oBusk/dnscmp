{
  "name": "dnscmp",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./dist/lib.js"
  },
  "bin": {
    "dnscmp": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/lib.ts --target=node --outfile=dist/lib.js && bun build src/cli.ts --target=node --outfile=dist/cli.js --banner='#!/usr/bin/env node'",
    "start": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
