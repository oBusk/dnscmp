{
  "name": "@dnscmp/repo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@dnscmp/core' build && bun run --filter '@dnscmp/providers' build && bun run --filter 'dnscmp' build",
    "start": "bun run --cwd packages/cli src/index.ts",
    "typecheck": "tsc --build"
  },
  "peerDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
